(function () {
  'use strict';
  // Compatibilidad con el HTML original. Las rutas /api ahora usan la base
  // persistente de ChatGPT Sites y la cookie segura de la sesión.
  window.WAS_ON_SHARED_STORAGE = true;
})();
